const state = {
    url: {
        user: 'api/user',
        role: 'api/role',
        menu: 'api/menu',
        log: 'api/logs',
        dept: 'api/dept'
    }
}

const getters = {}
const mutations = {}
const actions = {}

export default {
    namespaced: true,
    state,
    getters,
    mutations,
    actions
}