const state = {
    url: {
        user: 'api/user',
        role: 'api/role',
        menu: 'api/menu'
    }
}

const getters = {}
const mutations = {}
const actions = {}

export default {
    namespaced: true,
    state,
    getters,
    mutations,
    actions
}